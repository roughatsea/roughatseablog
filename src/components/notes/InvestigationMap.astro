<nav class="investigation-map" aria-label="Article sections">
  <p class="map-label">The investigation</p>
  <ol>
    <li><a href="#part-i"><span>I</span>The missing shelf</a></li>
    <li><a href="#part-ii"><span>II</span>The books that became ash</a></li>
    <li><a href="#part-iii"><span>III</span>Stillborn, withheld, enjoined</a></li>
    <li><a href="#part-iv"><span>IV</span>The edition that survived wounded</a></li>
    <li><a href="#part-v"><span>V</span>Escape routes</a></li>
    <li><a href="#part-vi"><span>VI</span>How to read an absence</a></li>
  </ol>
</nav>

<style>
  .investigation-map {
    width: min(900px, calc(100vw - 2rem));
    margin: 2.75rem 0 4rem 50%;
    padding: clamp(1.25rem, 3vw, 2rem);
    transform: translateX(-50%);
    border: 1px solid var(--border-color);
    border-radius: 12px;
    background: var(--surface-color);
    box-shadow: var(--glass-shadow);
    font-family: var(--font-sans);
  }

  .map-label {
    margin: 0 0 1rem;
    color: var(--text-secondary);
    font-size: 0.7rem;
    font-weight: 600;
    letter-spacing: 0.16em;
    text-transform: uppercase;
  }

  ol {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 0.65rem;
    margin: 0;
    padding: 0;
    list-style: none;
  }

  a {
    display: flex;
    align-items: center;
    min-height: 3.5rem;
    padding: 0.7rem 0.9rem;
    gap: 0.8rem;
    border: 1px solid var(--border-color);
    border-radius: 8px;
    color: var(--text-primary);
    text-decoration: none;
  }

  a:hover {
    border-color: var(--link-color);
    color: var(--link-color);
  }

  span {
    min-width: 2rem;
    color: var(--text-secondary);
    font-size: 0.7rem;
    font-weight: 600;
    letter-spacing: 0.08em;
  }

  @media (max-width: 640px) {
    ol {
      grid-template-columns: 1fr;
    }
  }
</style>
